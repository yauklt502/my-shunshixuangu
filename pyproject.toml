[project]
name = "sequoia-backtest"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "baostock>=0.9",
    "pandas>=2.0",
    "numpy>=1.26",
    "matplotlib>=3.8",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
